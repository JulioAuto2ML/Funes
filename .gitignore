# Build artifacts
build/
bin/
compile_commands.json
__pycache__/
*.pyc
node_modules/
third-party/whatsapp-mcp/whatsapp-bridge/whatsapp-bridge

# Local secrets and machine-local overrides
config/funes.local

# Documents contacts send whatsapp-autoresponder (see WHATSAPP_UPLOAD_DIR)
data/whatsapp-uploads/

# Runtime data
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS noise
.vscode/
.idea/
*.swp
.DS_Store

# ctest scratch, written when ctest is run from the repo root
Testing/
